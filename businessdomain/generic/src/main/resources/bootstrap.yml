spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      name: microservicio-generic
      profile: prod
      username: rguevarav
      password: Cd1526a2a8
      retry:
        max-attempts: 1
        initial-interval: 1000