spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      name: microservicio-rimasignacion
      profile: prod
      username: rguevarav
      password: Cd1526a2a8
      retry:
        max-attempts: 1
        max-interval: 100

eureka:
  client:
    service-url:
      defaultZone: http://172.27.250.43:8761/eureka